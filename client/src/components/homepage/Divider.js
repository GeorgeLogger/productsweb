const Divider = () => {
  return (
    <hr
      style={{
        width: "80%",
        height: "1px",
        border: "0",
        marginRight: "auto",
        marginLeft: "auto",
        color: "var(--clr-gold)",
      }}
    />
  );
};

export default Divider;
